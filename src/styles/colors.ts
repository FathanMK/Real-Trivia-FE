const colors = {
  dark: (alpha = '1') => `rgba(17,21,28,${alpha})`,
  light: (alpha = '1') => `rgba(229,228,226,${alpha})`,
  error: (alpha = '1') => `rgba(255,99,71,${alpha})`,
};

export default colors;
